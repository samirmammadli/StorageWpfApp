<Window x:Class="StorageWpfApp.View.FullReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StorageWpfApp.View"
        xmlns:conv="clr-namespace:StorageWpfApp.Converters"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        Background="#fafafa"
        Title="FullReportView" Height="450" Width="800">
    <Window.InputBindings>
        <KeyBinding 
                    Key="Esc"
                    Command="{Binding CloseWindow}" CommandParameter="{Binding ElementName=Consignments}" />
    </Window.InputBindings>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/StarterStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:NameAndLastNameConverter x:Key="Converter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <GroupBox Margin="5" Style="{x:Null}" Header="Накладные">
            <DataGrid
                  Height="Auto"
                  MinHeight="200"
                  ItemsSource="{Binding Invoices}"
                  HorizontalAlignment="Stretch"
                  AutoGenerateColumns="False"
                  HorizontalContentAlignment="Center"
                  EnableRowVirtualization="True"                      
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  AlternationCount="2">
                <DataGrid.CellStyle>
                    <Style BasedOn="{StaticResource {x:Type DataGridCell}}" TargetType="{x:Type DataGridCell}">
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="Padding" Value="0,5,0,0" />
                        <!--<Setter Property="Focusable" Value="False"/>-->
                        <Style.Triggers>
                            <Trigger Property="DataGridCell.IsSelected" Value="True">
                                <Setter Property="Background" Value="#c1c1c1" />
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="#c1c1c1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn IsReadOnly="True" Binding="{Binding Consignment.Product.Code}"  Header="Дата" MinWidth="100" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="True" Binding="{Binding Consignment.Code}"  Header="Номер накладной" MinWidth="150" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="True" Header="Клиент" MinWidth="100" Width="Auto">
                        <DataGridTextColumn.Binding>
                            <MultiBinding Converter="{StaticResource Converter}">
                                <Binding Path="Client.Name"/>
                                <Binding Path="Client.Surname"/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn IsReadOnly="True" Binding="{Binding Consignment.SellingPrice}" Header="Цена за еденицу" MinWidth="150" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="True" Binding="{Binding Consignment.Quantity}" Header="Доступное кол-во" MinWidth="150" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="False" Binding="{Binding Count}" Header="Количество" MinWidth="150" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="False" Binding="{Binding Discount}" Header="Скидка на ед." MinWidth="100" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="True" Binding="{Binding Sum}" Header="Сумма" MinWidth="150" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
